{% extends 'layouts/dashboard.html.twig' %}
{% import 'includes/admin/forms.html.twig' as form %}

{% block breadcrumb %}
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Edit PodcastLink</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="{{ path_for('home') }}">Home</a></li>
                        <li class="breadcrumb-item"><a href="{{ path_for('admin.index') }}">Dashboard</a></li>
                        <li class="breadcrumb-item"><a href="{{ path_for('admin.podcastLinks') }}">PodcastLinks</a></li>
                        <li class="breadcrumb-item active">Edit PodcastLinks</li>
                    </ol>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block content %}
    <section class="content">
        <div class="row">
            <div class="col-md-12">
                <div class="card card-info card-outline">
                    <div class="card-body">
                        <form data-thumb="true" action="{{ path_for('admin.podcastLinks.update', {id: item.id}) }}" method="POST" autocomplete="off">
                            {{ _csrf() }}
                            {{ _method('PUT') }}
                            <fieldset>
                                <br>
                                {{ form.input('reference', 'reference', errors.reference, input.reference ?? item.reference) }}
                            </fieldset>
                            <br>
                            <fieldset>
                                {{ form.textarea('description', 'description', errors.description, input.description ?? item.description) }}
                            </fieldset>
                            <div class="row">
                                <div class="col-lg-12">
                                    <br>
                                    <button class="btn btn-primary btn-block" type="submit">Submit</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block javascriptPlugins %}
    <script src="{{ base_url() ~ '/assets/admin/plugins/ckeditor/ckeditor.js' }}"></script>
    <script src="{{ base_url() ~ '/assets/admin/dist/js/main.js' }}"></script>
{% endblock %}
